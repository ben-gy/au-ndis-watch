{
  "name": "au-ndis-watch",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "data:collect": "node pipeline/collect.mjs",
    "data:aggregate": "node pipeline/aggregate.mjs",
    "data:all": "node pipeline/collect.mjs && node pipeline/aggregate.mjs"
  },
  "devDependencies": {
    "@types/leaflet": "^1.9.12",
    "@types/node": "^22.10.0",
    "jsdom": "^25.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vitest": "^3.2.6"
  },
  "dependencies": {
    "leaflet": "^1.9.4"
  }
}
